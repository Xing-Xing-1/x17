- **Documentation**
    - **Name**: `{{ name }}`
    - **Path**: `{{ path }}`
    - **Module**: `{{ module }}`
    - **Type**
        - **Functionalities**: `{{ type.functionalities | default([]) }}`
        - **Structures**: `{{ type.structures | default([]) }}`
        - **Roles**: `{{ type.roles | default([]) }}`
    - **Parent**
        - **Parents**: `{{ parents | default([]) | safe }}`
    - **Ref**
        - **Refs**: `{{ refs | default([]) }}`
    - **Desc**
        - `{{ desc | default('') }}`
    - **Usage**
        ```python
        {{ usage | default('# Example usage\ninstance = ClassName()') }}
        ```
    - **Thread Safe**: `{{ thread_safe | default(false) }}`
    - **Mutable**: `{{ mutable | default(false) }}`
    - **Lifecycle**: `{{ lifecycle | default([]) }}`
    - **Design Patterns**
        - `{{ design_patterns | default([]) }}`
    - **Deprecation**: `{{ deprecated | default(false) }}`
    - **Author**: `{{ author | default('') }}`

---

- **Class Parameters**
{% if class_params %}
{% for param in class_params %}
    - `{{ param.name }}`
        - **Type**: `{{ param.type }}`
        - **Desc**: `{{ param.desc }}`
        - **Optional**: `{{ param.optional }}`
        - **Default**: `{{ param.default }}`
{% endfor %}
{% else %}
    - None
{% endif %}

---

- **Attributes**
{% if attributes %}
{% for attr in attributes %}
    - `{{ attr.name }}`
        - **Type**: `{{ attr.type }}`
        - **Desc**: `{{ attr.desc }}`
{% endfor %}
{% else %}
    - None
{% endif %}

---

- **Instance Methods**
{% if instance_methods %}
{% for method in instance_methods %}
    - `{{ method.name }}`
        {% if method.desc %}
        - **Description**: {{ method.desc }}
        {% endif %}

        {% if method.params %}
        - **Params**
        {% for p in method.params %}
            - `{{ p.name }}`: `{{ p.type }}`, `{{ p.desc }}`
        {% endfor %}
        {% endif %}

        {% if method.returns %}
        - **Returns**
        {% for r in method.returns %}
            - `{{ r.type }}`: `{{ r.desc }}`
        {% endfor %}
        {% endif %}

        {% if method.error_handling %}
        - **Error Handling**
        {% for err in method.error_handling %}
            - `{{ err.type }}`: `{{ err.desc }}`
        {% endfor %}
        {% endif %}

{% endfor %}
{% else %}
    - None
{% endif %}

---

- **Static Methods**
{% if static_methods %}
{% for method in static_methods %}
    - `{{ method.name }}`
        {% if method.desc %}
        - **Description**: {{ method.desc }}
        {% endif %}
        {% if method.params %}
        - **Params**
        {% for p in method.params %}
            - `{{ p.name }}`: `{{ p.type }}`, `{{ p.desc }}`
        {% endfor %}
        {% endif %}

        {% if method.returns %}
        - **Returns**
        {% for r in method.returns %}
            - `{{ r.type }}`: `{{ r.desc }}`
        {% endfor %}
        {% endif %}

{% endfor %}
{% else %}
    - None
{% endif %}

---

- **Class Methods**
{% if class_methods %}
{% for method in class_methods %}
    - `{{ method.name }}`
        {% if method.desc %}
        - **Description**: {{ method.desc }}
        {% endif %}
        
        {% if method.params %}
        - **Params**
        {% for p in method.params %}
            - `{{ p.name }}`: `{{ p.type }}`, `{{ p.desc }}`
        {% endfor %}
        {% endif %}

        {% if method.returns %}
        - **Returns**
        {% for r in method.returns %}
            - `{{ r.type }}`: `{{ r.desc }}`
        {% endfor %}
        {% endif %}

{% endfor %}
{% else %}
    - None
{% endif %}